# 从冲突中学习：基于无标签成对比较的视觉—语言模型引导无源域自适应

> 稿件状态：JCR 1区期刊中文完整初稿（实验待补版）  
> 当前方法名称：成对冲突比较器（Pairwise Conflict Comparator）  
> 说明：文中所有 `[待补充：……]` 均为投稿前必须补齐的内容；现有数字均来自当前实验归档，不含虚构结果。正式投稿时需删除本说明及全部占位符。

## 术语表

| 统一术语 | 定义 | 本文用法 |
|---|---|---|
| 无源域自适应 | Source-Free Domain Adaptation，SFDA | 仅给定源模型和无标签目标数据，不访问源数据 |
| 任务模型 | Task model | 从源域训练模型初始化、在目标域适配的分类模型 |
| 视觉—语言模型 | Vision–Language Model，VLM | 本文实现中采用 CLIP |
| 一致样本 | Agreement sample | 任务模型与 VLM 的 Top-1 预测相同 |
| 冲突样本 | Conflict sample | 任务模型与 VLM 的 Top-1 预测不同 |
| 有用冲突 | Useful conflict | 两个冲突候选中至少一个等于真实类别；仅用于评估分析 |
| 成对冲突比较器 | Pairwise Conflict Comparator | 在任务候选与 VLM 候选之间进行二元可靠性判断的轻量网络 |
| 条件仲裁准确率 | Conditional arbitration accuracy | 在真实类别位于两个候选中的样本上，比较器选对一侧的比例 |

---

## 摘要

视觉—语言模型为无源域自适应提供了独立于源模型的语义知识，但现有双视角方法通常只接纳任务模型与视觉—语言模型预测一致的样本，并将预测冲突主要视为不可靠噪声。本文指出，这种一致性优先策略忽略了目标域中的一类关键监督来源：冲突样本虽然具有较高不确定性，但其候选标签中经常包含真实类别。对 Office-Home 全部12个迁移任务的诊断表明，任务模型与 CLIP 的预测冲突占目标样本的42.01%，其中69.49%的冲突至少包含一个正确候选。为在不访问源数据和目标标签的条件下利用该信息，本文提出成对冲突比较器。该方法首先由任务模型与 CLIP 的高置信一致样本构建类别均衡锚点库，再通过强增广诱导单侧预测翻转，自动生成“信任任务模型”或“信任 CLIP”的成对监督。随后，比较器综合双视角候选概率、预测熵、分类边际和锚点相似度，对真实冲突进行类别无关的可靠性判断。为缩小合成冲突与真实冲突之间的分布差异，本文进一步采用按方向分组的难度匹配、类别方向平衡和跨轮重放，并通过固定覆盖率选择抑制比较器置信度随适配轮次漂移。当前 A→C 开发实验显示，在完全相同的20%高可靠冲突子集上，比较器在第2—4轮分别取得51.28%、40.61%和46.03%的仲裁准确率，较同子集上的最佳单一候选分别提高4.39、9.09和15.08个百分点。完整实验将在 Office-Home、VisDA-C 和 `[待补充：第三个数据集名称]` 上进一步验证该方法的总体准确率、跨任务稳定性和计算效率。上述结果表明，模型间冲突不仅是需要抑制的噪声，也是一种可以通过无标签比较学习主动恢复的目标域监督信号。

**关键词：** 无源域自适应；视觉—语言模型；冲突样本；伪标签；成对比较；可靠性估计

### Highlights（投稿系统如要求）

- Office-Home 中42.01%的目标样本表现为任务模型—VLM预测冲突。
- 69.49%的冲突至少包含一个正确候选，揭示了被一致性策略舍弃的监督来源。
- 无标签单侧翻转为任务候选与VLM候选的相对可靠性提供自动监督。
- 在A→C固定20%冲突子集上，比较器连续三轮超过两个单一候选。

> **[待补充图形摘要]**  
> 建议用一条横向流程呈现“冲突发现 → 一致锚点 → 单侧翻转监督 → 成对比较 → 高可靠冲突回收”，并在右侧突出“同子集仲裁增益”和“无目标标签参与训练”。

---

## 1 引言

深度分类模型在训练分布和部署分布存在偏移时往往出现明显性能下降。无监督域适应通过利用无标签目标数据缓解这一问题，但多数方法需要持续访问源数据，因此受到隐私、存储和数据授权条件的限制。无源域自适应仅保留已经训练好的源模型，并在不重新访问源数据的条件下完成目标域适配，更符合许多真实部署场景的约束。[待补充引用：无监督域适应综述、SFDA代表性工作、数据隐私或源数据不可访问场景]

近年来，大规模视觉—语言模型凭借跨数据和跨任务训练形成了较强的开放语义知识，为 SFDA 提供了源模型之外的第二种预测视角。典型方法将任务模型的领域相关判别能力与 VLM 的跨域语义能力结合，通过一致伪标签、软知识蒸馏或联合自训练改善目标域表征。[待补充引用：CLIP原始论文、VLM引导域适应、DUET及最接近的工作] 然而，现有方法通常将两种模型的预测一致性作为可靠性的主要依据：当两者给出相同类别时，样本被接纳为硬伪标签；当两者产生冲突时，样本则被延迟、弱化或直接排除。

这种处理隐含了一个未经充分检验的假设，即冲突样本的错误风险高于其潜在价值。我们的系统诊断给出了不同结论。在 Office-Home 的12个迁移任务中，任务模型与 CLIP 的冲突并非少量边界样本，而是占全部目标样本的42.01%；更重要的是，69.49%的冲突样本至少有一个候选预测正确。这意味着，仅依赖一致样本会舍弃大量可恢复的目标域监督。与此同时，始终信任任务模型、始终信任 CLIP、比较置信度、原型一致性或邻域支持均不能可靠完成逐样本仲裁。因此，真正的难点不是判断“冲突是否有用”，而是在无标签条件下判断“当前冲突应该信任哪一侧，以及何时应该拒绝决策”。

本文从这一问题出发，将任务模型与 VLM 的冲突处理重新表述为类别无关的成对可靠性比较。与直接平均两个预测分布或为冲突强制分配硬标签不同，我们使用高置信一致样本生成可验证的合成冲突监督：对于一个在弱视图上得到双模型一致预测的样本，如果强增广只使任务模型发生翻转，则原一致类别为 VLM 侧的可靠候选；反之，如果只使 VLM 翻转，则原一致类别为任务模型侧的可靠候选。该机制不需要目标真实标签，并将难以获得的真实冲突标注转化为可自动构造的二元比较任务。

基于上述思路，本文提出成对冲突比较器。该方法具有四个核心设计。第一，使用类别均衡的高置信一致锚点库，使少数类和多数类具有相近的参考容量。第二，将双视角候选概率、熵、分类边际和候选类别的锚点相似度组织为16维类别无关证据，从而使比较规则能够跨类别迁移。第三，通过真实冲突的无标签统计对合成冲突进行难度匹配，并对“信任任务模型”和“信任 VLM”两个方向实施严格平衡，减少合成训练分布偏差。第四，采用固定覆盖率的高边际选择，使每轮只处理相同比例的最可靠冲突，避免绝对阈值受置信度膨胀影响。

本文的主要贡献如下。

1. 我们系统刻画了 VLM 引导 SFDA 中的冲突样本，证明冲突既普遍存在，又包含大量未被一致性伪标签利用的正确候选，为双视角自适应提供了新的问题视角。
2. 我们提出一种完全不依赖目标标签的成对冲突监督生成机制，通过双模型一致锚点和强增广单侧翻转学习“信任任务模型”与“信任 VLM”的相对可靠性。
3. 我们提出结合类别均衡锚点、类别无关成对证据、方向条件难度匹配、跨轮重放和固定覆盖率选择的完整冲突仲裁框架，并将仲裁结果一致地注入硬伪标签和软蒸馏目标。
4. 我们建立同子集、固定覆盖率的冲突评估协议，避免使用不同样本集合比较方法准确率。现有开发结果显示，比较器在连续三轮中均超过同一冲突子集上的任务模型和 CLIP；完整跨数据集结果将在 `[待补充：主结果表编号]` 中报告。

---

## 2 相关工作

### 2.1 无源域自适应

SFDA 的基本目标是在无法访问源数据时，仅利用源模型和无标签目标数据学习目标模型。现有方法大致包括基于信息最大化的方法、基于伪标签的方法、基于邻域结构的方法、基于对比学习的方法以及基于教师—学生更新的方法。[待补充引用：SHOT、NRC、AdaContrast、CoWA、PLUE、SCLM等代表性工作] 这些方法的共同难点是伪标签误差会随训练迭代累积，尤其是在域偏移较大或类别边界不均衡时。本文不改变 SFDA 的无源约束，而是研究当额外引入 VLM 视角后，如何从两个预测器的冲突中恢复可靠监督。

### 2.2 视觉—语言模型引导的域适应

CLIP 等 VLM 通过图像—文本对比预训练获得了较强的跨域语义表示，并被用于零样本识别、测试时适应和源自由适应。[待补充引用：CLIP、prompt learning、VLM-guided TTA/SFDA] 相关方法通常通过文本原型、软分布蒸馏或双模型伪标签将 VLM 知识注入任务模型。DUET 进一步利用任务模型与 CLIP 的一致预测建立伪标签，并联合优化目标模型与 CLIP 视觉编码器。[待补充引用：DUET正式论文] 这类方法有效利用了双视角共识，但对预测不一致样本的处理仍较保守。本文与它们的区别在于，我们不把冲突简单视为低可信样本，而是显式学习两个候选之间的相对可靠性。

### 2.3 不一致学习、协同训练与候选标签学习

多视角学习和协同训练长期利用不同预测器之间的一致与互补信息；分歧也被用于主动学习、模型诊断和错误发现。[待补充引用：co-training、disagreement-based learning、query-by-committee] 另一方面，部分标签学习假设真实类别位于候选集合中，并通过候选消歧获得监督。[待补充引用：partial-label learning及深度部分标签学习] 与这些工作不同，本文既不预设一个固定候选集合，也不直接把两个冲突候选同时作为正标签。我们将冲突表述为二元可靠性比较，并使用无标签增强诱导的单侧翻转自动构造比较监督。

### 2.4 不确定性与伪标签选择

置信度、预测熵、分类边际和邻域一致性常用于筛选伪标签。[待补充引用：uncertainty-aware pseudo-labeling、calibration、selective prediction] 然而，不同模型的 softmax 概率通常不可直接比较，单一不确定性指标也难以覆盖类别结构和域偏移。本文因此将模型内概率证据、全分布不确定性和类条件锚点支持联合编码为成对特征，并通过学习而非手工规则完成选边。固定覆盖率选择进一步借鉴选择性预测中的风险—覆盖思想，但不使用目标标签确定选择阈值。[待补充引用：selective classification/risk–coverage]

---

## 3 方法

### 3.1 问题定义

设源域训练完成的任务模型为 \(f_T=g\circ h\)，其中 \(h\) 为特征提取器，\(g\) 为分类器。目标域仅提供无标签数据集

\[
\mathcal{D}_t=\{x_i\}_{i=1}^{N},
\]

类别数为 \(C\)。给定冻结文本侧的视觉—语言模型 \(f_V\)，任务模型和 VLM 对样本 \(x_i\) 分别产生类别概率

\[
\mathbf{p}_i^T=f_T(x_i),\qquad
\mathbf{p}_i^V=f_V(x_i).
\]

对应的 Top-1 候选为

\[
a_i=\arg\max_c p_{i,c}^{T},\qquad
b_i=\arg\max_c p_{i,c}^{V}.
\]

由此定义一致样本集合和冲突样本集合：

\[
\mathcal{A}=\{i\mid a_i=b_i\},\qquad
\mathcal{C}=\{i\mid a_i\neq b_i\}.
\]

传统双视角伪标签方法主要从 \(\mathcal{A}\) 中学习。本文的目标是在不访问源数据和目标标签的条件下，为 \(i\in\mathcal{C}\) 学习一个可靠性函数

\[
q_i=Q(\mathbf{z}_i)\in[0,1]^2,
\]

其中 \(q_{i,0}\) 和 \(q_{i,1}\) 分别表示信任任务候选 \(a_i\) 与 VLM 候选 \(b_i\) 的概率，\(\mathbf{z}_i\) 为类别无关的成对证据。

### 3.2 方法概览

方法整体流程如图1所示。每轮目标域适配首先由任务模型和 VLM 生成概率分布，并按基线流程完成首轮类别先验校准。随后，高置信一致样本被用于构建类别均衡锚点库；这些样本的强增广预测进一步产生单侧翻转的合成冲突。成对比较器使用合成冲突训练，并在真实冲突上输出信任任务模型或信任 VLM 的分数。为保证训练监督接近真实冲突，我们使用真实冲突的无标签熵和边际统计进行难度匹配，并在两个信任方向之间实施平衡。最终，每轮仅选择比较器边际最高的固定比例冲突，将胜方预测同时用于硬伪标签和软蒸馏目标，其余冲突维持基线的一致性和软知识约束。

> **[待补充图1：总体框架图]**  
> 建议内容：左侧为任务模型与 CLIP 双视角；中间上方为一致锚点库及强增广单侧翻转；中间下方为16维成对证据与比较器；右侧为固定覆盖率选择以及 hard/soft 双路径注入。图中明确标注“目标标签仅用于离线评估，不参与训练、阈值选择或模型选择”。

### 3.3 首轮类别先验校准

为减轻目标域预测的类别频率偏置，本文沿用首轮类别先验校准。对于任一预测矩阵 \(\mathbf{P}\in\mathbb{R}^{N\times C}\)，计算经验类别先验

\[
\pi_c=\frac{1}{N}\sum_{i=1}^{N}P_{i,c},
\]

并得到校准概率

\[
\bar P_{i,c}=
\frac{P_{i,c}/(\pi_c+\epsilon)^{\alpha}}
{\sum_{k=1}^{C}P_{i,k}/(\pi_k+\epsilon)^{\alpha}}.
\]

该操作只在第1轮执行，并分别作用于任务模型和 VLM。当前实现设置 \(\alpha=0.8\)、\(\epsilon=10^{-5}\)。需要强调的是，先验校准属于基础适配路径；本文的主要创新位于后续冲突监督生成与成对仲裁模块。最终消融中应单独报告其影响。

### 3.4 类别均衡一致锚点库

可靠的比较监督需要一个不依赖真实标签的参考集合。我们从预测一致且双侧置信度较高、熵较低的样本中选取锚点。样本 \(x_i\) 被纳入锚点候选集合需满足

\[
a_i=b_i,quad
\max\mathbf{p}_i^T\ge\tau_T,quad
\max\mathbf{p}_i^V\ge\tau_V,
\]

\[
H(\mathbf{p}_i^T)\le\eta_T,quad
H(\mathbf{p}_i^V)\le\eta_V.
\]

当前实现还要求先验校准前后均保持双视角一致，且公共预测类别不发生变化，以排除仅由校准操作制造的伪一致。每个候选的可靠性分数定义为

\[
r_i=\max\mathbf{p}_i^T+\max\mathbf{p}_i^V
-\lambda_H\left[H(\mathbf{p}_i^T)+H(\mathbf{p}_i^V)\right].
\]

对于每个类别，按照 \(r_i\) 从高到低保留最多 \(K\) 个锚点，分别建立任务特征锚点库与 VLM 图像特征锚点库。类别均衡设计避免高频类别在参考集合中占据过大比例。当前配置为 \(K=8\)、\(\tau_T=0.7\)、\(\tau_V=0.8\)、\(\eta_T=\eta_V=0.5\)、\(\lambda_H=1\)。

### 3.5 基于单侧翻转的合成冲突监督

真实冲突没有可用标签，无法直接监督比较器。本文利用高可信一致样本在强增广下的非对称稳定性自动生成成对监督。设某锚点在弱视图上的公共类别为 \(y_i\)，其强增广视图记为 \(x_i^s\)。任务模型与 VLM 在强视图上的预测分别为 \(a_i^s\) 和 \(b_i^s\)。我们保留以下两种单侧翻转：

\[
a_i^s\neq y_i,\quad b_i^s=y_i
\Rightarrow \text{trust VLM},
\]

\[
a_i^s=y_i,\quad b_i^s\neq y_i
\Rightarrow \text{trust Task}.
\]

若两侧均保持 \(y_i\)，则没有形成冲突；若两侧同时离开 \(y_i\)，则无法确定哪一侧可靠。两种情况均不用于比较器监督。为排除退化翻转，翻转候选需满足最小候选概率和最大翻转边际约束：

\[
p_B\ge\tau_r,\qquad p_B-p_{y_i}\le\tau_m.
\]

当前实现采用 \(\tau_r=0.10\) 和 \(\tau_m=0.60\)。这一构造的关键在于，监督标签来自双模型对同一强增广视图的相对稳定性，而不是目标真实类别。

### 3.6 类别无关成对证据

对于真实冲突，记 \(A=a_i\) 为任务候选，\(B=b_i\) 为 VLM 候选。比较器输入为16维证据向量：

\[
\begin{aligned}
\mathbf{z}_i=[
&p_i^T(A),p_i^T(B),p_i^V(A),p_i^V(B),\\
&H(\mathbf{p}_i^T),H(\mathbf{p}_i^V),m_i^T,m_i^V,\\
&s_i^{T,A},s_i^{T,B},s_i^{V,A},s_i^{V,B},\\
&v_i^{T,A},v_i^{T,B},v_i^{V,A},v_i^{V,B}],
\end{aligned}
\]

其中 \(m_i^T\) 和 \(m_i^V\) 为各模型内部 Top-1 与 Top-2 的概率差，仅作为模型内不确定性指标，不参与候选集合扩张；\(s_i^{M,c}\) 表示样本在模型 \(M\) 的特征空间中与类别 \(c\) 锚点的 Top-\(k\) 平均余弦相似度；\(v_i^{M,c}\) 表示相应类别是否存在有效锚点。当前设置 \(k=3\)。该表示不直接编码类别编号，因此比较器学习的是“证据更支持哪一侧”，而不是记忆特定类别。

成对比较器由两层多层感知机构成，每个隐藏层包含64个单元、GELU 激活和0.1 dropout，输出两个 logits：

\[
\mathbf{q}_i=\operatorname{softmax}(Q_{\theta}(\mathbf{z}_i)).
\]

在合成冲突集合 \(\mathcal{S}\) 上，优化二元交叉熵：

\[
\mathcal{L}_{cmp}
=-\frac{1}{|\mathcal{S}|}\sum_{i\in\mathcal{S}}
\log q_{i,y_i^{cmp}}.
\]

### 3.7 方向条件难度匹配与重放

强增广产生的合成冲突可能比真实冲突更容易，直接训练会造成分布错配。我们使用当前轮全部真实冲突的无标签统计作为参考，在任务熵、VLM 熵、任务边际和 VLM 边际四个维度上分别计算标准分数：

\[
z_{i,d}=\frac{z_{i,d}^{syn}-\mu_d^{real}}
{\sigma_d^{real}+\epsilon}.
\]

只有四个维度均满足 \(|z_{i,d}|\le\tau_z\) 的合成样本才被保留。该筛选分别在“信任任务模型”和“信任 VLM”两个方向上执行，随后将样本数下采样至相同，以防比较器向某一方向坍缩。当合格样本过少时，保留平均绝对标准分数最小的至少 \(n_{min}\) 个样本。当前设置 \(\tau_z=1.5\)、\(n_{min}=16\)。

为减轻跨轮遗忘，每个方向保存最近64个匹配后的合成样本。训练批次中25%来自历史重放，75%来自当前轮样本。比较器在第2—4轮持续保留参数和优化器状态，每轮使用 Adam 优化200步，批大小为64，学习率为 \(10^{-4}\)，权重衰减为 \(10^{-4}\)。

### 3.8 固定覆盖率冲突选择

比较器输出的信任边际定义为

\[
\Delta_i=|q_{i,0}-q_{i,1}|.
\]

初步实验发现，随着训练轮次增加，比较器的绝对边际会系统性增大，使固定阈值对应的覆盖率从30.26%扩张至89.98%。因此，本文不使用跨轮固定绝对门槛，而是在每轮冲突集合内按 \(\Delta_i\) 降序排列，仅选择前 \(\rho\) 比例：

\[
\mathcal{R}=\operatorname{Top}_{\rho}
\{\Delta_i\mid i\in\mathcal{C}\}.
\]

当前开发设置为 \(\rho=20\%\)。该排序不读取目标标签，并保证各轮干预规模可比。对 \(i\in\mathcal{R}\)，选择

\[
\hat y_i=
\begin{cases}
a_i,&q_{i,0}\ge q_{i,1},\\
b_i,&q_{i,0}<q_{i,1}.
\end{cases}
\]

未被选择的冲突继续接受基线一致性训练和 VLM 软蒸馏，但不进入硬伪标签集合。

### 3.9 与目标域适配目标的联合优化

选中的冲突同时修改硬伪标签和软蒸馏目标，以避免两个训练路径给出互相矛盾的监督。目标模型的总损失写为

\[
\mathcal{L}
=\lambda_{cls}\mathcal{L}_{cls}
+\lambda_{con}\mathcal{L}_{con}
+\lambda_{kl}\mathcal{L}_{kl},
\]

其中 \(\mathcal{L}_{cls}\) 为已接纳样本上的交叉熵，\(\mathcal{L}_{con}\) 为弱增广与强增广之间的一致性损失，\(\mathcal{L}_{kl}\) 为任务模型输出与 VLM 软目标之间的 KL 散度。对已仲裁冲突，\(\mathcal{L}_{cls}\) 使用比较器选择的候选，\(\mathcal{L}_{kl}\) 使用胜方模型的完整概率分布。当前实现设置 \(\lambda_{cls}=0.4\)、\(\lambda_{con}=0.2\)、\(\lambda_{kl}=0.4\)。

### 3.10 算法流程

**算法1：无标签成对冲突学习**

1. 输入源模型、VLM、无标签目标集和适配轮数；
2. 每轮计算任务概率、VLM 概率及双侧特征；
3. 第1轮进行类别先验校准；
4. 从高置信一致样本构建类别均衡锚点库；
5. 对锚点强增广并提取单侧翻转，生成二元合成冲突；
6. 依据真实冲突的无标签熵和边际统计进行方向条件难度匹配；
7. 平衡两个信任方向，与历史重放共同训练成对比较器；
8. 在真实冲突上计算信任分数和比较边际；
9. 选择比较边际最高的固定比例冲突，更新硬伪标签和软目标；
10. 使用分类、一致性和软蒸馏损失更新目标模型；
11. 输出适配后的目标模型。

> **[待补充：算法复杂度]**  
> 报告比较器参数量、单轮额外前向次数、显存增加、训练时间增加及推理时是否移除比较器。建议给出相对 DUET 的百分比开销。

---

## 4 实验设置

### 4.1 数据集

**Office-Home。** Office-Home 包含 Art、Clipart、Product 和 Real-World 四个视觉域、65个类别，共形成12个有向迁移任务。本文在全部12个任务上报告平均分类准确率。[待补充引用：Office-Home数据集原始论文]

**VisDA-C。** `[待补充：按最终实验协议描述 Synthetic→Real 设置、训练/验证划分、12个类别和 mean per-class accuracy 指标。]` [待补充引用：VisDA-C]

**第三个数据集。** `[待补充：建议选择 DomainNet-126 或另一个具有明显域偏移的数据集，并说明选择理由、域组合、类别数、样本数和评估指标。]`

### 4.2 对比方法

对比方法应覆盖以下四组：

1. Source-only 和目标域无适配；
2. 经典 SFDA 方法，如 SHOT、NRC、AdaContrast、CoWA；
3. 最新强 SFDA 方法，如 `[待补充：投稿前两年内的代表方法]`；
4. VLM 引导方法，包括 DUET 及 `[待补充：与本文最接近的 VLM-SFDA/TTA 方法]`。

所有可复现方法应使用相同源模型、目标数据划分和骨干网络重新运行；无法重新运行的方法需明确标注引用论文结果。`[待补充：方法版本、代码链接、超参数来源及是否使用作者公开权重。]`

### 4.3 评价指标

除标准目标域分类准确率外，本文报告以下冲突专用指标：

- **冲突率：** \(|\mathcal{C}|/N\)；
- **有用冲突率：** 真实类别位于 \(\{a_i,b_i\}\) 的冲突比例，仅作 oracle 诊断；
- **同子集仲裁准确率：** 在比较器实际选择的同一子集上，分别计算任务候选、VLM 候选和比较器选择的准确率；
- **候选上限：** 同一子集中只要任一候选正确即视为正确；
- **条件仲裁准确率：** 在候选上限成立的样本中，比较器选中正确候选的比例；
- **覆盖率：** 比较器实际处理的冲突数占当前轮冲突总数的比例；
- **伪标签精度与覆盖：** 联合评价新增监督质量和数量，避免只报告高精度低覆盖结果。

主要实验使用 `[待补充：3或5]` 个随机种子，报告均值±标准差。方法间差异使用 `[待补充：配对t检验、Wilcoxon符号秩检验或跨任务bootstrap]`，并报告95%置信区间和效应量。所有超参数仅在预先指定的开发任务上确定，其余任务和数据集保持锁定。

### 4.4 实现细节

Office-Home 当前实现采用 ResNet-50 任务骨干和 CLIP ViT-B/32。目标模型使用 SGD，初始学习率0.01、动量0.9、权重衰减 \(10^{-3}\)，批大小64；每个任务训练4轮，每轮4个 epoch。CLIP 文本侧冻结，仅以学习率 \(10^{-6}\) 更新视觉侧。比较器配置见表1。

**表1 成对冲突比较器的默认配置**

| 组件 | 设置 |
|---|---:|
| 每类锚点数 | 8 |
| Task / CLIP 锚点置信阈值 | 0.7 / 0.8 |
| Task / CLIP 锚点熵阈值 | 0.5 / 0.5 |
| 成对特征维数 | 16 |
| 比较器隐藏层 | 2层，每层64维 |
| 激活 / dropout | GELU / 0.1 |
| 每轮比较器更新步数 | 200 |
| 比较器优化器 | Adam，学习率 \(10^{-4}\) |
| 难度匹配阈值 | \(\tau_z=1.5\) |
| 每方向最少匹配样本 | 16 |
| 每方向重放容量 | 64 |
| 重放批次比例 | 25% |
| 冲突选择覆盖率 | 20% |

代码实现已通过318项测试，另有5项因运行环境或可选依赖被跳过。`[待补充：硬件型号、CUDA/PyTorch版本、单次训练耗时、代码与随机种子发布地址。]`

---

## 5 实验结果

### 5.1 冲突样本是普遍且可利用的目标域信号

我们首先在 Office-Home 全部12个任务上分析任务模型与 CLIP 的预测关系。表2显示，冲突率在23.99%—63.83%之间，宏平均为42.57%。按样本数加权后，共有19,647个冲突，占46,764个目标样本观测的42.01%。其中13,653个冲突至少包含一个正确候选，对应69.49%的有用冲突率。该结果说明，冲突不是可以忽略的少数异常，而是双视角 SFDA 中占比接近一半的重要样本群体。

**表2 Office-Home 12个任务中的冲突结构（%）**

| 任务 | 初始任务模型（Source）准确率 | CLIP准确率 | 冲突率 | 有用冲突/全部 | 有用冲突/冲突 |
|---|---:|---:|---:|---:|---:|
| A→C | 44.88 | 59.43 | 58.60 | 33.61 | 57.35 |
| A→P | 67.18 | 84.23 | 34.69 | 27.46 | 79.16 |
| A→R | 74.57 | 85.36 | 27.52 | 22.31 | 81.07 |
| C→A | 53.73 | 74.78 | 50.14 | 35.89 | 71.57 |
| C→P | 61.70 | 84.23 | 39.69 | 32.17 | 81.04 |
| C→R | 65.00 | 85.36 | 36.91 | 30.41 | 82.40 |
| P→A | 51.92 | 74.78 | 50.39 | 35.56 | 70.56 |
| P→C | 40.78 | 59.43 | 63.83 | 36.93 | 57.86 |
| P→R | 72.30 | 85.36 | 29.58 | 24.17 | 81.69 |
| R→A | 64.52 | 74.78 | 39.60 | 27.65 | 69.82 |
| R→C | 45.93 | 59.43 | 55.88 | 30.68 | 54.90 |
| R→P | 77.90 | 84.23 | 23.99 | 18.77 | 78.22 |
| 宏平均 | 60.03 | 75.95 | 42.57 | 29.63 | 72.14 |

进一步分解发现，Source 错而 CLIP 对的样本占全部样本22.42%，Source 对而 CLIP 错的样本占6.77%，两者均错但预测不同的样本占12.82%。因此，始终信任 CLIP 虽然具有较强平均优势，但仍会系统性破坏一部分源模型原本正确的预测；一个有效方法必须同时完成 CLIP 纠偏、Source 保护和双错拒绝。

> **[待补充图2：Office-Home冲突结构可视化]**  
> 建议绘制12个任务的堆叠条形图，分为“Source对/CLIP错”“Source错/CLIP对”“双错冲突”和“一致样本”。另配一幅宏平均或加权平均环形图。图注必须说明真实标签只用于分析。

### 5.2 手工可靠性规则不足以完成冲突仲裁

表3比较了多种无需目标标签的静态选择规则。候选集合召回率达到72.14%，说明两个 Top-1 候选包含较高比例的真实类别；但始终选择 CLIP 已是最强静态基线，准确率为55.33%。比较置信度、原型相似度、邻域支持及三种证据的联合规则均未超过该基线。这一结果排除了“只需增加一个简单阈值或相似度规则”即可解决冲突的解释，并支持学习类别无关的多证据比较函数。

**表3 Office-Home 12任务冲突仲裁诊断的宏平均结果（%）**

| 方法 | 冲突仲裁准确率 |
|---|---:|
| 始终选择 Source | 16.81 |
| 始终选择 CLIP | **55.33** |
| 选择较高置信度 | 54.03 |
| 原型一致性 | 43.00 |
| 邻域支持 | 41.19 |
| 三角联合规则 | 50.46 |
| 候选集合召回率（oracle上限指标） | 72.14 |
| 本文成对冲突比较器 | `[待补充：12任务平均]` |

### 5.3 难度匹配缩小了合成—真实冲突差异

合成冲突能否代表真实冲突，直接决定比较器的迁移能力。表4给出 A→C 第2轮的分布统计。原始合成冲突的任务模型分类边际为0.3036，高于真实冲突的0.2250；匹配后下降至0.2113。任务熵也由2.1914调整至2.4630，更接近真实冲突的2.3855。VLM 边际和熵呈现相同的接近趋势。当前轮147个合成冲突中保留62个，两个信任方向各31个。第3轮和第4轮分别保留96个和58个匹配样本，并维持严格1:1方向平衡。

**表4 A→C第2轮合成与真实冲突分布**

| 样本集合 | Task边际 | Task熵 | CLIP边际 | CLIP熵 |
|---|---:|---:|---:|---:|
| 原始合成冲突 | 0.3036 | 2.1914 | 0.4423 | 1.1604 |
| 真实冲突 | 0.2250 | 2.3855 | 0.4961 | 1.0777 |
| 匹配后合成冲突 | 0.2113 | 2.4630 | 0.4438 | 1.0879 |

> **[待补充图3：合成—真实冲突分布图]**  
> 建议分别绘制Task熵、CLIP熵、Task边际和CLIP边际的核密度图或小提琴图，比较匹配前、真实冲突和匹配后。需在全部12个任务或至少三个代表任务上展示，而不仅是A→C。

### 5.4 A→C中比较器在相同冲突子集上连续三轮超过两个单一候选

不同选择方法往往处理不同难度的样本，直接比较各自选中子集的准确率会产生偏差。为此，我们固定每轮真实冲突集合，并按比较器边际选取相同的20%样本，再分别评估任务候选、CLIP 候选和比较器选择。表5显示，比较器在第2—4轮均取得最高准确率。相对于同子集中的较强单一候选，其增益分别为4.39、9.09和15.08个百分点。条件仲裁准确率保持在74.36%—82.84%，表明当真实类别确实位于两个候选中时，比较器能够较高概率识别正确一侧。

**表5 A→C固定20%冲突子集上的仲裁结果（seed=2020，%）**

| 轮次 | Task | CLIP | 比较器 | 较强候选→比较器 | 候选上限 | 条件仲裁准确率 |
|---|---:|---:|---:|---:|---:|---:|
| 第2轮 | 15.02 | 46.89 | **51.28** | +4.39 | 61.90 | 82.84 |
| 第3轮 | 22.42 | 31.52 | **40.61** | +9.09 | 53.94 | 75.28 |
| 第4轮 | 30.95 | 30.95 | **46.03** | +15.08 | 61.90 | 74.36 |

这一结果是当前方法最直接的有效性证据：比较器不是通过选择更容易的样本获得表面高精度，而是在完全相同的冲突子集上超过两个输入候选。尤其在第4轮，Task 和 CLIP 在选中子集上的准确率均为30.95%，而比较器达到46.03%，说明模型学习到的并非固定偏向某一侧的规则。

> **[待补充图4：跨轮风险—覆盖曲线]**  
> 横轴为覆盖率10%、20%、40%、60%、80%和100%，纵轴为同子集仲裁准确率；每轮分别绘制Task、CLIP和比较器曲线。主文至少报告12任务平均曲线及95%置信区间。

### 5.5 固定覆盖率避免绝对阈值造成干预规模漂移

在使用绝对比较边际0.20时，A→C 的覆盖率从第2轮30.26%增长到第4轮89.98%，而选中子集准确率由48.91%下降至36.47%。轨迹分析表明，真实冲突上的比较器整体准确率变化较小，但输出边际持续扩大。因此，覆盖扩张主要来自置信度尺度漂移，而不是可仲裁样本真实增加。固定20%覆盖率将三轮干预规模锁定为20%，使方法比较和训练干预更稳定。

**表6 A→C选择策略的阶段性结果（%）**

| 策略 | 第2/3/4轮覆盖率 | 最终准确率 | 第4轮过程最佳 |
|---|---|---:|---:|
| 绝对门槛0.20，hard | 30.3 / 74.6 / 90.0 | 73.01 | 73.17 |
| 固定20%，hard | 20 / 20 / 20 | **73.20** | **73.31** |
| 固定20%，soft-only | 20 / 20 / 20 | 73.10 | 73.29 |

hard 与 soft-only 的最终差异仅为0.10个百分点，单个开发任务不足以判断哪种注入方式更优。完整消融必须在全部任务和多个随机种子上进行。本文默认使用 hard 方案，是因为它同时保证硬伪标签和软蒸馏目标的一致性。

### 5.6 与先进方法的总体比较

**表7 Office-Home 12任务分类准确率（%，均值±标准差）**

| 方法 | A→C | A→P | A→R | C→A | C→P | C→R | P→A | P→C | P→R | R→A | R→C | R→P | 平均 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Source-only | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` |
| SHOT | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` |
| NRC | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` |
| `[待补充强基线1]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` |
| DUET | 73.60 | 90.40 | 91.00 | 83.60 | 90.70 | 90.90 | 82.70 | 73.70 | 91.20 | 83.60 | 74.00 | 91.20 | 84.72 |
| DUET-FCP匹配对照 | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` |
| **本文方法** | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` | **`[待补充]`** |

`[待补充结果段：必须回答本文方法在多少个任务上优于DUET、平均提升、标准差、95%置信区间及统计显著性。不要只报告最佳seed或用目标标签选择的oracle peak。]`

**表8 VisDA-C及第三数据集结果**

| 方法 | VisDA-C mean per-class accuracy | `[第三数据集指标]` | 参数/计算开销 |
|---|---:|---:|---:|
| Source-only | `[ ]` | `[ ]` | `[ ]` |
| `[强SFDA基线]` | `[ ]` | `[ ]` | `[ ]` |
| DUET | `[ ]` | `[ ]` | `[ ]` |
| DUET-FCP匹配对照 | `[ ]` | `[ ]` | `[ ]` |
| **本文方法** | **`[ ]`** | **`[ ]`** | `[ ]` |

`[待补充结果段：说明跨数据集泛化，尤其是固定20%覆盖率是否直接迁移，若重新调整需写明无标签调参协议。]`

### 5.7 消融实验

**表9 方法组件消融（全部任务、多seed）**

| 先验校准 | 类别均衡锚点 | 单侧翻转监督 | 难度匹配 | 方向平衡 | 重放 | 固定覆盖率 | 总体准确率 | 仲裁准确率 |
|---|---|---|---|---|---|---|---:|---:|
|  |  |  |  |  |  |  | `[ ]` | `[ ]` |
| ✓ |  |  |  |  |  |  | `[ ]` | `[ ]` |
| ✓ | ✓ | ✓ |  |  |  |  | `[ ]` | `[ ]` |
| ✓ | ✓ | ✓ | ✓ |  |  |  | `[ ]` | `[ ]` |
| ✓ | ✓ | ✓ | ✓ | ✓ |  |  | `[ ]` | `[ ]` |
| ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |  | `[ ]` | `[ ]` |
| ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | **`[ ]`** | **`[ ]`** |

建议进一步补充以下小型消融：

- 锚点数 \(K\in\{2,4,8,16\}\)；
- 覆盖率 \(\rho\in\{10\%,20\%,30\%,40\%\}\)；
- 比较器特征组：概率、熵/边际、Task锚点、VLM锚点；
- 无重放、重放比例12.5%、25%、50%；
- hard、soft-only、只仲裁不训练三种注入方式；
- 合成冲突使用同一强视图与跨视图构造的对照；
- 每轮重置比较器与跨轮持久比较器的对照。

### 5.8 类别级与失败案例分析

`[待补充图5：类别级收益热图。横轴为迁移任务或类别，纵轴为Task、CLIP、比较器及最终模型；重点检查car/person/truck等困难类别是否发生收益交换。]`

`[待补充图6：定性案例。每组显示原图、Task候选及概率、CLIP候选及概率、锚点近邻、比较器决策和真实标签。至少包括：(a) CLIP纠正Task；(b) Task保护免受CLIP错误影响；(c) 两者均错且比较器应拒绝；(d) 比较器失败案例。]`

`[待补充失败分析：统计错误来源，包括候选集合不含真值、锚点缺失、双模型共同类别偏差、低覆盖困难冲突，以及跨轮置信度漂移。]`

### 5.9 计算效率

**表10 计算开销比较**

| 方法 | 可训练参数 | 单轮时间 | 总训练时间 | 峰值显存 | 推理额外开销 |
|---|---:|---:|---:|---:|---:|
| DUET | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` |
| 本文方法 | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` |
| 相对变化 | `[ ]` | `[ ]` | `[ ]` | `[ ]` | `[ ]` |

`[待补充说明：比较器只在训练时用于伪标签刷新，若部署推理不需要比较器，应明确写出“零额外推理开销”。]`

---

## 6 讨论

本文的核心发现是，VLM 引导 SFDA 中的预测冲突具有双重属性。它们一方面比一致样本更困难，包含相当比例的双错情形；另一方面，大多数冲突仍保留至少一个正确候选。因此，简单排除冲突会损失监督覆盖，直接信任某一模型又会引入结构化错误。成对冲突比较器在这两种极端之间提供了第三条路径：它不尝试从全部类别中重新分类，而是判断现有两个候选中哪一侧得到更强的相对证据。

当前结果支持该设计确实学到了可迁移的选边信号。在 A→C 连续三轮固定20%冲突子集上，比较器均优于 Task 和 CLIP，且优势随适配推进由4.39个百分点扩大至15.08个百分点。由于三个方法在完全相同的样本上评估，这一增益不能由样本难度差异解释。相比之下，置信度、原型、邻域和手工联合规则均未超过始终选择 CLIP，说明有效仲裁需要同时建模多源证据及其非线性关系。

单侧翻转监督是方法能够在无标签条件下训练的关键。其基本假设是：高置信双模型一致样本的弱视图公共标签可以作为局部稳定参考，而强增广造成的单侧翻转揭示了两个模型对该样本的相对鲁棒性。这一监督并不保证每个合成标签都正确，因此我们没有直接扩大合成池，而是进一步执行难度匹配和方向平衡。实验中，匹配后的合成冲突在熵和边际上更接近真实冲突，同时消除了信任方向失衡。这表明，无标签增强不仅可用于一致性正则，也可用于构造模型间可靠性比较任务。

固定覆盖率选择解决了另一个容易被忽略的问题：神经比较器的输出尺度会随训练变化，固定边际阈值并不对应固定风险。A→C 中绝对阈值使覆盖率由30.26%扩张到89.98%，造成大量低质量冲突在后期进入训练。按轮次排序并固定覆盖率后，干预规模保持稳定，也使风险—覆盖比较具有统一口径。该策略的目的不是证明20%在所有数据集上都最优，而是建立一种不读取目标标签、对输出尺度变化更稳健的选择机制。

本文仍存在若干边界。第一，当前已归档的完整比较器证据主要来自 Office-Home A→C、seed=2020；全任务、多种子和跨数据集结果必须在投稿前完成。第二，比较器只能在两个现有候选中选边；当 Task 和 VLM 均预测错误时，它无法恢复候选集合之外的真实类别，只能依赖低边际拒绝减少伤害。第三，合成冲突来自强增广诱导的单侧翻转，其分布与自然域偏移下的真实冲突仍可能存在高阶差异。第四，固定覆盖率将干预风险转化为超参数 \(\rho\)，需要验证同一设置能否跨数据集迁移。第五，当前 A→C 的局部仲裁优势尚需通过完整匹配对照证明能够稳定转化为端到端总体准确率增益。

这些限制也指出了后续研究方向。更强的冲突学习可进一步引入显式拒绝建模、候选外恢复机制和无标签风险估计，但这些扩展必须提供独立信息，而不能仅重新组合 Task 与 VLM 的原始置信度。本文建立的同子集仲裁评估、风险—覆盖曲线和合成—真实冲突匹配分析，可以作为后续方法研究的统一诊断工具。

---

## 7 结论

本文研究了视觉—语言模型引导无源域自适应中长期被一致性策略忽略的冲突样本问题。Office-Home 全任务诊断显示，冲突占目标样本的42.01%，其中69.49%至少包含一个正确候选，说明冲突是一类规模大且具有潜在价值的目标域信号。为在无标签条件下利用该信号，本文提出成对冲突比较器，通过高置信一致锚点、强增广单侧翻转、类别无关成对证据、方向条件难度匹配、跨轮重放和固定覆盖率选择，学习任务模型与 VLM 候选之间的相对可靠性。当前开发实验中，比较器在连续三轮的相同20%冲突子集上均超过两个单一候选，最高取得15.08个百分点的相对增益。完整跨任务、多种子和跨数据集实验补齐后，本文将进一步检验这一局部仲裁能力能否稳定转化为整体 SFDA 性能提升。总体而言，本研究表明，模型间冲突不应仅被视为需要过滤的噪声，也可以被转化为可学习的目标域监督。

---

## 数据与代码可用性

`[待补充：代码仓库链接、公开时间、许可证、配置文件、随机种子、预训练模型和结果日志下载地址。]`

## 伦理与局限声明

本文仅使用公开视觉分类数据集。目标域真实标签仅用于离线评价，不参与模型训练、样本选择、超参数选择或检查点选择。`[待补充：数据许可核查、模型偏差与潜在误用说明。]`

## 作者贡献

`[待补充：按CRediT taxonomy填写 Conceptualization、Methodology、Software、Validation、Formal analysis、Writing等贡献。]`

## 利益冲突

作者声明不存在利益冲突。`[如实际情况不同，请修改。]`

## 参考文献

> 当前保留引用占位符，避免生成未经核验的参考文献。投稿前应至少覆盖：SFDA基础与综述、SHOT、NRC、AdaContrast、CoWA、PLUE、SCLM、CLIP、prompt learning、VLM引导SFDA/TTA、DUET、co-training、disagreement-based learning、partial-label learning、selective classification/risk–coverage、Office-Home、VisDA-C和第三数据集原始论文。

`[待补充：经多源核验的完整参考文献列表，格式按目标期刊要求。]`

---

## 投稿前必须完成的证据清单

### A. 会直接决定论文是否达到JCR 1区标准

- [ ] Office-Home 12任务的本文方法与完全匹配 DUET-FCP 对照；
- [ ] 至少3个随机种子的均值、标准差和95%置信区间；
- [ ] VisDA-C完整结果；
- [ ] 至少一个额外数据集或更困难的域偏移设置；
- [ ] 与最新强方法的公平比较；
- [ ] 组件消融及交互消融；
- [ ] 统计显著性与效应量；
- [ ] 计算开销；
- [ ] 类别级结果和失败案例；
- [ ] 目标标签未参与训练、阈值和模型选择的审计说明。

### B. 建议形成的主文图表

1. 图1：方法总览；
2. 图2：12任务冲突结构；
3. 图3：合成—真实冲突分布匹配；
4. 图4：风险—覆盖曲线；
5. 图5：类别级收益热图；
6. 图6：成功与失败案例；
7. 表7：Office-Home主结果；
8. 表8：跨数据集主结果；
9. 表9：核心消融；
10. 表10：计算效率。

### C. 不能在结果补齐前使用的表述

- “彻底解决了冲突样本问题”；
- “在所有数据集和任务上稳定有效”；
- “显著优于当前最先进方法”；
- “具有统计显著的总体提升”；
- 将目标标签选出的最佳检查点作为普通无标签 SFDA 主结果。
