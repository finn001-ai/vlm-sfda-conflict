# DUET 开发历史归档

这个目录保留 2026-08-26 前开发过程中的 `duet_*`、DAC handoff、PLMatch revision 以及对应测试和运行脚本。

它们只用于追溯历史实验，不是当前可执行入口。当前正式方法是仓库中的 DCR，基线实现只保留一份 `src/methods/oh/plmatch.py`。

归档内保留原来的相对路径，方便与历史 commit 和日志对照。
